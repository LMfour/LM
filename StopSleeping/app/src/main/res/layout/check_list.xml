<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageButton
        android:id="@+id/chk_back_img"
        android:layout_width="330dp"
        android:layout_height="94dp"
        android:layout_marginTop="8dp"
        android:background="@android:color/transparent"
        android:scaleType='fitXY'
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/menu_round_btn" />

    <TextView
        android:id="@+id/chk_day"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:text="2022/03/24"
        app:layout_constraintStart_toStartOf="@+id/chk_time"
        app:layout_constraintTop_toBottomOf="@+id/chk_time" />

    <TextView
        android:id="@+id/chk_time"
        android:layout_width="124dp"
        android:layout_height="34dp"
        android:layout_marginStart="32dp"
        android:layout_marginTop="18dp"
        android:text="오후 02:42:39"
        android:textColor="@color/black"
        android:textSize="20dp"
        app:layout_constraintStart_toStartOf="@+id/chk_back_img"
        app:layout_constraintTop_toTopOf="@+id/chk_back_img" />

    <ImageButton
        android:id="@+id/chk_trash_img"
        android:layout_width="22dp"
        android:layout_height="31dp"
        android:layout_marginTop="28dp"
        android:layout_marginEnd="32dp"
        android:background="@android:color/transparent"
        android:scaleType='fitXY'
        app:layout_constraintEnd_toEndOf="@+id/chk_back_img"
        app:layout_constraintTop_toTopOf="@+id/chk_back_img"
        app:srcCompat="@drawable/trashcan" />

</androidx.constraintlayout.widget.ConstraintLayout>